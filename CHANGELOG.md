# 変更履歴

利用者に関係する機能追加・変更・不具合修正を記録します。未リリースの変更は次のリリース時にバージョンと公開日を付けてまとめます。

## 未リリース

### 改善

- QuickLookの選択追従をShellの変更通知に基づく処理へ変更しました。選択・フォーカスなどの変化がない間は、200 msごとの選択確認を行いません。連続する通知はまとめて処理し、送信中に変わった選択にも追従します。
- 作業状態の保存準備を変更通知に基づく処理へ変更しました。保存対象が変わらない間は毎秒のスナップショット・JSON生成を行わず、連続する変更は約1秒待ってまとめます。終了時の最終保存と、保存失敗後の自動再試行は維持します。
- ブックマークの重複判定に辞書を使い、多数登録時の復元を高速化しました。特にWSLパスの繰り返し解析による一時メモリ確保を削減します。パスの同一視ルール、最初に登録した名前・パス・順序、削除後の再登録は維持します。
- ブックマークの追加・削除時は対象の行だけを更新し、既存のボタン・メニュー・アイコンを再利用するようにしました。削除した行のアイコン待機も終了し、取得が遅れていても不要な行を保持し続けないようにしました。

### 追加

- 通常版の終了・ビルド・起動をまとめて行う`update.cmd`を追加しました。
- ブックマークとドライブをホイールクリックすると、フォーカス中のペインに新規タブを追加して開くようにしました。左クリックは従来どおり選択中のタブで開きます。
- 設定画面に「ブックマーク・ドライブを新規タブで開くマウスボタン」を追加しました。中ボタン・右ボタン・サイドボタン1・サイドボタン2・無効を選択でき、保存後すぐに反映されます。

### 修正

- Deleteキーや右クリックメニューによる削除の確認画面を閉じたあと、元のタブのファイル一覧へフォーカスを戻すようにしました。
- QuickLookへの送信を選択変更などで取り消した際、QuickLook 4.5の受信処理が停止し、標準エクスプローラーも含めてプレビューできなくなる原因となる切断処理を修正しました。
- 表示中のタブを閉じたとき、同じペインで直前に表示していたタブへ戻るようにしました。閉じたタブは飛ばし、表示履歴のあるタブが残っていない場合は右隣（右端なら左隣）を表示します。
- ファイル一覧で名前を変更しているときに、左右の矢印キーが効かない問題を修正しました。編集欄へキー入力を直接渡し、カーソル移動やShift・Ctrlを併用した操作を処理できるようにしました。
