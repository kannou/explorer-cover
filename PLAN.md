# explorer_cover 開発計画

作成日: 2026-09-19
最終更新日: 2026-09-20

## 目的と進め方

プロジェクト名は`explorer_cover`。Windows Shellの機能を活用する外枠という位置付けを表す。C#の名前空間とプロジェクトファイルには`ExplorerCover`、アプリ表示名と実行ファイル名には`explorer_cover`を使用する。

Windows Shellのファイル操作・閲覧機能を極力再利用しながら、ウィンドウ構成と状態管理を自分の使い方に合わせたファイラーを作る。

採用構成は **C#＋WPF＋IExplorerBrowser** とする。最初の到達点は、左右2つのシェルビューを実際に操作できる小さな試作とする。試作をユーザーが試用し、その結果に基づいて計画と優先順位を見直す。試用前にタブや高度な機能の実装まで進めない。

## 最終的に実現したいこと

| 要件 | 方針 |
|---|---|
| 2ペイン | 左右の独立した閲覧領域と、幅を変更できる境界を用意する |
| 各ペインのタブ | ペインごとに追加・切替・閉じる操作を用意する |
| タブのマウス操作 | ドラッグで並べ替え、タブ上のクリックで閉じる、タブバーの空白部分のクリックで新規タブを開く |
| 状態復元 | 各タブの場所、選択中タブ、左右の幅を保存・復元する |
| Explorer相当の操作 | 右クリック、ドラッグ＆ドロップ、コピー・貼り付け等にシェルの機能を活用する |
| キーボード操作 | ファイル操作の標準キーと、タブ・ペイン操作の独自キーを両立する |
| 左側のサイドビュー | ウィンドウ左端に共通のブックマーク一覧とドライブ一覧を置く。クリックで操作対象ペインの選択中タブへ移動する |
| ドライブ容量 | ドライブ名・パス、総容量・空き容量・使用率を表示する |
| タブ内の移動 | 各タブに独立した戻る・進む履歴を持ち、戻る・進む・ひとつ上へ移動するボタンを用意する |
| クイックビュー | 一覧でファイルを選択してSpaceを押すと、インストール済みのQuickLookでプレビューする |
| 設定画面 | ショートカットキーとタブを閉じるマウスボタンの変更・保存・初期値への復帰を用意する |

最新のExplorerの外観や、すべてのシェル拡張との完全互換は前提にしない。必要な互換性は試用で確認する。

## 全体計画表

| 段階 | 内容 | 成果物・完了条件 | 状態 |
|---|---|---|---|
| 0. 計画 | 方針、試作範囲、確認項目を記録する | 本計画書 | 完了 |
| 1. 開発基盤 | 利用可能な.NET SDKと対象OS・CPUを確認し、WPFプロジェクトを作成する | ビルドと起動ができる最小アプリ、実行手順 | 完了 |
| 2. 1ペインの技術検証 | HwndHostでIExplorerBrowserをホストし、移動・リサイズ・破棄を接続する | 指定フォルダーを表示して移動できる1ペイン | 完了 |
| 3. 小さな試作 | 左右2ペイン化し、パス入力、フォーカス、キー入力を整える | 下記の試作完了条件を満たすアプリと検証記録 | 完了 |
| 4. ユーザー試用・見直し | 実際の操作感、互換性、不満点を記録し、継続方針を決める | 試用結果と改訂した本計画書 | 初回試用・計画更新済み |
| 5a. 状態と操作の基盤 | ペイン・タブ・履歴の状態モデルと共通コマンド、キー割り当ての仕組みを作る | ボタンとキーが同じ操作を呼び、操作対象と入力中の扱いが統一される | 完了（2026-09-20） |
| 5b. タブと移動 | 各ペインのタブ追加・切替・閉じる・複製、タブごとの戻る・進む・親へ移動を実装する | 履歴がタブ間で混ざらず、ボタンとキーで移動できる | 完了・初回試用済み（2026-09-20） |
| 5b-2. タブのマウス操作 | ドラッグによる並べ替え、クリックによる対象タブの終了、空白部分からの新規タブ作成を追加する | タブの状態を維持して並べ替えられ、ドラッグ終了で閉じる・追加が誤発動しない | 完了・初回試用済み（2026-09-20） |
| 5c. QuickLook連携 | 選択ファイル取得と既存QuickLookの呼び出しを実装する | 一覧のSpaceでプレビューでき、入力・名前変更・IMEを妨げない | 実装・主要検証完了、ユーザー試用待ち（2026-09-20） |
| 6a. 左サイドビュー | 共通ブックマークとドライブ・容量一覧を追加する | クリックで操作対象タブへ移動でき、容量取得中もUIが応答する | 次の実装予定（5cの試用後） |
| 6b. 保存・復元 | タブ・パス・選択・ペイン幅・サイドビュー幅とブックマークを保存する | 再起動して作業構成とブックマークが戻る | レイアウトと状態モデル完成後、ここで試用 |
| 7. 設定画面 | 共通コマンドのショートカットとタブを閉じるマウスボタンの変更、競合確認、保存・初期値復帰を実装する | 変更した割り当てが反映され、再起動後も維持される | 対象操作と保存処理が揃った後 |
| 8. 実用化 | 試用で見つかった問題、DPI対応、配布方法などを整える | 日常利用に向けた確認と起動・更新手順 | 各段階の試用結果に応じて調整 |

日程は最初の埋め込み検証の結果を見て決める。特にシェル連携の不確実性を先に解消する。

2026-09-19時点: 小さな試作と最終キー検証を完了。ユーザーが`trial.cmd`を試用し「いい感じ」との感想を受領した。[起動手順](README.md)、[検証記録](docs/VERIFICATION.md)、[試用メモ](docs/TRIAL.md)を参照。現構成を継続し、次はタブ、その後に状態復元を実装する進め方を提案する。

## 次の到達点：タブと移動操作、その後に作業状態の復元

1. **状態モデルと共通コマンドを分離する（5a）。** ウィンドウ、ペイン、タブ、履歴をUIから分離する。キーはコマンドへ割り当てる形にして、機能ごとのキー判定を追加し続ける構成を避ける。設定画面は後で追加するが、割り当ての変更を受け入れる構造はこの時点で作る。
2. **タブと戻る・進む・親へ移動を実装して試用する（5b）。** 左右独立の追加・切替・閉じる・複製を用意する。キー初期値案はCtrl+T、Ctrl+W、Ctrl+Tab／Ctrl+Shift+Tab、Alt+Left／Right／Up。F6は左右切替のまま維持する。最後の1タブは閉じずに残す。ビューの保持方法はタブごとの履歴・選択状態・スクロール位置と資源使用量を検証して決める。
3. **タブのマウス操作を追加して試用する（5b-2）。** 同一ペイン内の並べ替え、対象タブを閉じる操作、空白部分からの新規タブ作成を用意する。タブ順と操作対象の扱いを保存・復元より先に確定する。
4. **QuickLookとサイドビューを追加する（5c・6a）。** 既存の選択取得・コマンド・移動処理を利用する。サイドビューを含めた最終的な配置を決めてから、レイアウトの保存を仕上げる。
5. **保存・復元を実装して再度試用する（6b）。** タブ順、場所、選択中タブ、左右幅の比率、サイドビュー幅、ブックマークをJSONへ保存する。変更をまとめて保存し、安全なファイル置換で書き込み途中の破損を避ける。存在しないパスや読めない設定でも起動できるようにする。作業状態とユーザー設定を分離し、形式にバージョンを付ける。
6. **設定画面を追加して試用する（7）。** 既存の共通コマンドと設定保存処理にUIを接続する。その他の追加機能は試用結果で優先順位を決める。

直近の到達点（5b）は、左右それぞれに複数タブを開き、タブ単位の戻る・進む・親への移動が使えること。その次の到達点（6b）は、QuickLookと左サイドビューが使え、終了・再起動でタブ・場所・選択・幅・ブックマークが戻ること。既存のシェル操作と日本語入力を各段階で維持する。

2026-09-20: 段階5aを実装・検証済み。WPF非依存の状態モデルと共通コマンド、JSONによるキー変更を追加し、既存の移動ボタンとキーを接続した。設定画面や作業状態の保存は未実装。[基盤の設計と検証](docs/FOUNDATION.md)を参照。次は段階5b。

2026-09-20: 段階5aを`c2972c8`にコミットした後、段階5bを実装・検証。タブごとにビューを保持し、履歴はアプリが管理する方式を採用。新規タブはペインの起動時の場所、複製は現在地を開き、どちらも新しい履歴で開始する。[タブと移動の設計・検証](docs/TABS.md)を参照。次は`trial.cmd`で試用し、追加要望の段階5b-2（タブのマウス操作）を実装・再試用した後、段階5cのQuickLook連携へ進む。

## 追加要望の仕様と実装順の理由（2026-09-20）

現在の進捗: タブのマウス操作と隙間の解消を試用・承認後、`ef99786`へコミットした。段階5cのQuickLook連携を実装し、Store版4.5.0.0で表示・開閉・キー変更・IME共存を確認した。[QuickLookの仕様と検証](docs/QUICKLOOK.md)を参照。次は5cを試用し、段階6aの左サイドビューへ進む。

### タブごとの戻る・進む・ひとつ上へ（5b）

- 以前「数日使って改善」に挙げた戻る・進む・親へ移動と同じ要望。タブの状態設計に直結するため、タブ実装と同時に前倒しする。
- ボタンは各ペインの選択中タブに作用する。戻れない・進めない・親がない場合は無効にする。
- 一覧内の移動、パス入力、ブックマーク、ドライブ、親へ移動を、同じ履歴更新処理へ接続する。
- A→B→戻る→Cでは進む履歴を破棄する。移動失敗は履歴の確定・更新に含めず、戻る／進むによる移動を通常移動として重複登録しない。
- シェル内の履歴とアプリの履歴を二重に正としない。5a〜5bで管理主体を決める。初期方針では履歴は起動中に保持し、終了後の履歴復元は今回の保存対象に含めない。
- 完了確認: タブ切替で履歴が混ざらない、履歴分岐、失敗した移動、ドライブのルートでの親ボタン、ボタンとキーの一致。

### タブのマウス操作（5b-2、2026-09-20追加）

段階5bのタブ基盤を拡張し、QuickLook連携より前に実装・試用する。タブ順と操作対象の扱いを先に整え、段階6bの保存・復元の手戻りを減らす。実装・Coreテスト・実画面検証を完了。検証結果は[マウス操作の記録](docs/TAB-MOUSE.md)を参照。

| 操作 | 初期仕様 |
|---|---|
| 並べ替え | タブ見出しを左ボタンでドラッグし、同じペイン内の任意の位置へ移動する |
| タブを閉じる | 設定したマウスボタンで、クリックしたタブを閉じる。初期値は中クリック。設定で変更可能 |
| 空白から追加 | クリックしたタブバーのペインに新規タブを追加して選択する。左ダブルクリックを使用する |

**並べ替え**

- 初期範囲は同一ペイン内。左右ペイン間の移動や別ウィンドウへの切り離しは別の拡張として扱う。
- 左ボタンを押したタブを選択する。移動距離のしきい値でクリックとドラッグを区別し、ドラッグ中は挿入位置を表示する。
- ドロップ成功時だけ順序を確定する。Escape、バー外へのドロップ、マウスキャプチャの喪失では順序を変更しない。ドラッグ開始時のタブ選択は維持する。
- タブが横にあふれた場合はバー端でスクロールできるようにし、見えない位置にも移動可能にする。
- タブID、現在地、履歴、編集中のパス、一覧の選択・スクロールを維持する。ビューの破棄・再生成で順序を変えない。
- 新しい順序をCtrl+Tab／Ctrl+Shift+Tabに即座に反映する。段階6bではその順序を保存・復元する。

**閉じる・新規タブ**

- 閉じるマウスボタンは段階5b-2から設定データとして扱う。選択肢は中・右・サイドボタン1／2・無効。左ボタンはタブ選択とドラッグ用に確保する。
- 段階5b-2ではJSONから読み込み、再起動時に反映する。段階7で同じ設定に変更・保存・初期値復帰の画面を接続する。作業状態の保存とは分離し、マウス判定へ直接ボタンを固定しない。
- 変更後は旧ボタンで閉じない。「無効」は見出しのクリックによる終了だけを解除し、×ボタンやキーによる終了は維持する。不正な設定は既存のキー設定と同様に初期値へ戻して通知する。
- 割り当てはタブ見出し上だけに適用する。右ボタンを選んだ場合は閉じる操作と見出しのコンテキストメニューを同時に発動させない。一覧内のシェルの右クリックやサイドボタン操作は変更しない。
- 非選択タブを閉じても選択中タブは維持する。選択中タブを閉じる場合は右隣、右端なら左隣を選択する。最後の1タブは閉じない。
- 閉じる操作は同じ見出し上で押して離した場合に実行し、押したまま外へ移動した場合は取り消す。
- 新規タブは既存の＋ボタンと同じ操作を呼び、該当ペインの起動時の場所を開く。
- 「空白部分」は見出しと操作ボタンを除いたタブバー内とする。見出し内部の余白、スクロールバー、パス欄、一覧は対象にしない。
- ドラッグ終了を新規タブ作成のクリックとして扱わない。既存の＋ボタンも引き続き利用できる。

**実装順と確認**

1. `PaneState`にタブIDを保持した順序変更を追加する。対象タブを明示して閉じられる操作経路を整え、キーの「選択中タブを閉じる」と共通化する。
2. `BrowserPane`の見出し順をモデルへ追従させる。ビューの保持・解放処理は順序変更と分離する。
3. 閉じるボタンの設定モデル・検証・JSON読み込みを用意し、設定に応じた終了と空白から追加のクリック判定を接続する。
4. ドラッグのしきい値、挿入表示、端でのスクロール、確定・取消を接続する。
5. 先頭・末尾・同じ位置への移動、選択／非選択タブの終了、最後のタブ保護、左右独立、履歴・選択・スクロール保持、キー切替の順序、バー外ドロップ・Escape・誤発動防止を検証して試用する。再起動後の復元は段階6bで検証する。
6. 閉じるボタンの変更・旧ボタンの解除・無効化・不正設定時の初期値復帰・再起動時の読み込みを検証する。設定画面からの変更と保存は段階7で検証する。

### SpaceによるQuickLook（5c）

- 試用後の変更（2026-09-20）: プレビュー中は一覧の選択ファイルに表示を追従させる。`Switch`命令で既に表示されている内容だけを更新し、閉じた後の再表示と選択変更によるQuickLook本体の起動を防ぐ。未選択・複数選択・フォルダーでは表示を維持し、編集中は更新しない。

- ユーザーのPCにはQuickLookがインストール済み。プレビュー描画はQuickLookに任せる。
- 一覧にフォーカスがあり、単一のファイルが選択されている場合にSpaceで呼び出す。パス入力、名前変更、IME変換中にはSpaceを奪わない。キーリピートとQuickLook自身のキー監視による二重起動も確認する。
- 選択ファイルはシェルから取得する。未選択・複数選択・ファイルパスを取得できない項目は初期実装では呼び出さない。閉じる操作後の一覧へのフォーカス復帰も検証する。
- ユーザー環境はStore版4.5.0.0。公開されている名前付きパイプの通信形式に合わせて直接送信する方式を採用し、追加のBridge.exeなしで表示を確認した。独自のWPF外枠ではQuickLookの自動検出による二重トグルは発生しなかった。[QuickLook公式の連携資料](https://github.com/QL-Win/QuickLook/wiki/Develop,-build-and-integrate)
- 起動先の自動検出と、見つからない場合の手動指定を用意する。初期は設定データ、段階7で設定画面から指定可能にする。起動失敗でファイラーが終了しないようにする。
- この段階に置く理由: 選択取得とキーの振り分けを固めてから実装し、後の設定画面では同じプレビューコマンドの割り当てを編集できるようにする。
- 完了確認: 日本語・空白入りパスのファイルをSpaceで表示、QuickLook未起動／検出失敗、編集・IMEとの共存、プレビューを閉じた後の操作。
- 検証結果: Coreテスト18件、通信テスト5件が成功。実画面でSpace／変更したF8による開閉、長押し・タブ変更取消、未選択・複数選択・フォルダー除外、ZIP表示、パス入力・名前変更・IMEとドラッグ移動の共存、起動先不明時の通知を確認した。未起動時の再試行・起動失敗・タイムアウトは独立した通信テストで確認。実物のQuickLookを停止した状態からの自動起動は未検証として試用項目に残す。

### 左サイドビューとドライブ容量（6a）

- 初期配置は **共通サイドビュー｜左ペイン｜右ペイン**。ブックマークとドライブの2セクションを表示する。
- ブックマークとドライブのクリック先は、直前まで操作していたペインの選択中タブとする。サイドビューへフォーカスが移っても操作対象ペインを維持し、対象が分かる表示を付ける。これは初期仕様として採用し、試用で調整する。
- ブックマークは表示名とパスを持ち、現在の場所の登録・削除・名前変更を可能にする。クリックで既存タブを移動し、そのタブの履歴にも反映する。到達できないパスは登録を消さずにエラー表示する。
- ドライブは名前・ルートパス・総容量・空き容量・使用率を表示する。クリックでルートへ移動できるようにする。
- 容量取得はUIを止めずに行い、未挿入・切断・アクセス不可は「取得できません」等で表示する。更新と接続／切断時の一覧更新を用意し、遅いドライブへの問い合わせが重ならないようにする。
- この段階に置く理由: タブの操作対象と移動履歴を再利用し、サイドビューの幅も含めて次の段階で保存する。ブックマーク保存には6bで共通化する保存処理を使う。
- 完了確認: 左右それぞれを対象にした移動、履歴への反映、ブックマーク編集、容量表示と更新、切断ドライブでも画面が応答すること。ブックマークの再起動後の保持は6bで確認する。

### ショートカットを変更する設定画面（基盤5a・画面7）

- タブ・移動・ペイン切替・QuickLook等を、識別子を持つ共通コマンドとして5aから定義する。ボタンとショートカットは同じコマンドを使う。
- 画面では操作名・現在のキーを一覧表示し、割り当て変更・解除・初期値へ戻す・保存を用意する。反映後と再起動後の両方で動作を確認する。
- 同じ入力範囲での重複、使えないキー、テキスト入力との競合を検出する。通常の文字入力やIMEを保護し、設定画面自身のキー入力をファイル操作として実行しない。
- シェルへ渡しているコピー・削除等のキーも、対応可能なものは共通コマンドへ接続して変更対象にする。シェル拡張内部などアプリで制御できないキーは、変更可能であるかのように表示しない。対応可否を実装時に検証し、制限を明示する。
- この順にする理由: キー判定の構造は最初に統一し、対象コマンドと設定保存が揃った後に画面を作る。後から各イベントのキー判定を置き換える作業を避ける。
- 完了確認: 変更したキーで正しい操作対象に一度だけ実行されること、旧キーの扱い、競合エラー、初期値復帰、再起動後の設定保持、編集・IMEとの共存。

## 最初のゴール：小さな試作

以下は完成済みの最初の試作に対する範囲と検証記録であり、追加要望を除外するものではない。

### 含める範囲

- 1つのWPFウィンドウに左右2つのIExplorerBrowserを表示する。
- 中央の境界をドラッグして左右の幅を変更できる。
- 各ペインにパス入力欄を設け、ローカルフォルダーへ移動できる。
- 一覧内のフォルダー移動をパス欄に反映する。
- 存在しないパスへの移動に失敗してもアプリが終了せず、失敗が分かる。
- 左右の一覧とパス欄の間でフォーカスを移せる。
- 標準の右クリック、ドラッグ＆ドロップ、基本的なファイル操作キーを確認する。
- ビルド・起動手順と、動作確認済み項目・既知の制限を記録する。

### この時点では含めない範囲

- タブ、終了時の状態保存、起動時の状態復元。
- 独自のファイル一覧描画、独自のコピー処理・進捗画面。
- 検索、プレビュー、お気に入り、独自の履歴UI。
- 全ショートカットの設定画面、インストーラー、既定ファイラーへの登録。
- 仮想フォルダー、クラウド、ネットワーク、各種シェル拡張の全面的な互換性保証。

### 完了条件

1. 手順に従ってビルド・起動できる。
2. 左右に別のローカルフォルダーを表示し、独立して移動できる。
3. リサイズとペイン幅変更後も表示・操作が成立する。
4. 下記の必須確認を実施し、結果を記録する。基本操作を妨げる問題は解消する。
5. 既知の制限と試用方法が文書化され、ユーザーに試用を引き渡せる。

試作の完成と、ユーザーによる試用の完了は別の到達点として扱う。試作を渡した時点では、試用評価を完了扱いにしない。

## 試作の動作確認表

作業用に作成したテストフォルダーとファイルで確認する。結果は「未確認／成功／問題あり／対象外」と再現条件で記録する。

| 優先度 | 確認項目 | 観点 | 結果 |
|---|---|---|---|
| 必須 | 起動・終了 | 繰り返し起動・終了して例外が発生しない | 成功（3回、解放ログ確認） |
| 必須 | フォルダー移動 | 左右独立の移動、パス欄の追従、存在しないパス | 成功 |
| 必須 | レイアウト | ウィンドウのリサイズ、最大化、ペイン幅変更 | 成功 |
| 必須 | 右クリック | ファイル・フォルダー・一覧背景でメニューを開き、基本操作を実行できる | 成功 |
| 必須 | 左右間のドラッグ＆ドロップ | コピー・移動、修飾キーによる動作の違い | 成功（Ctrlでコピー、通常操作で移動） |
| 必須 | Explorerとのドラッグ＆ドロップ | アプリからExplorer、Explorerからアプリの両方向 | 成功 |
| 必須 | コピー・貼り付け | Ctrl+C／X／V、左右間とExplorerとの受け渡し | 成功 |
| 必須 | 名前変更・削除 | F2、Delete、ごみ箱への移動 | 成功 |
| 必須 | キー入力・フォーカス | 一覧とパス欄の切替、文字入力中に操作キーを誤処理しない | 成功（F6、Ctrl+L、Esc、名前変更中のCtrl+A） |
| 必須 | 日本語 | 日本語を含むパスとファイル名、IMEによる入力・名前変更 | 成功（パス欄と名前変更欄でIME変換・確定を確認） |
| 追加 | 表示倍率 | 利用環境のDPIで表示やクリック位置にずれがない | 未確認 |
| 追加 | 普段使うシェル拡張 | 利用中の右クリック拡張等が必要な範囲で動く | 未確認 |
| 追加 | その他の場所 | 必要に応じてネットワーク・クラウド等を確認する | 未確認 |

## 技術方針と先に確認するリスク

- **外枠とシェル連携を分離する。** WPFが配置・パス欄・将来のタブを担当し、専用のホストクラスがCOMと子ウィンドウを管理する。
- **ネイティブホストの寿命を明確にする。** STA、イベント購読解除、ビュー破棄、COM参照の解放を検証する。
- **キー入力の転送を早期に検証する。** WPFとシェルビューのどちらが処理するかを整理し、二重実行や取りこぼしを防ぐ。
- **一覧上にWPF要素を重ねる構成を避ける。** HwndHostの描画・重ね合わせの制約を踏まえて外枠を設計する。
- **ファイル操作はシェルに任せる。** 将来、独自ボタンから操作する必要が出た場合はIFileOperationを検討する。
- **タブとビューの対応は試用後に決める。** タブごとにビューを保持する方式と、ペイン内で再利用する方式のメモリ・復元・操作感を比較する。
- **状態保存はモデルで管理する。** 保存対象は場所、タブ順、選択中タブ、ペイン幅の比率、サイドビュー幅、ブックマーク、キー設定とする。作業状態とユーザー設定を分離し、破損した設定や存在しなくなった場所への対応を段階6b〜7で扱う。

## 試用後の見直し

ユーザーが試作を操作した後、以下を記録して段階5以降を更新する。

| 見直し項目 | 記録内容 |
|---|---|
| 操作感 | 良かった点、ストレスがある操作、再現手順 |
| 互換性 | 必須の右クリック項目、ドラッグ＆ドロップ、利用する場所での問題 |
| キーボード | よく使う操作、希望する割り当て、競合 |
| 次の優先順位 | 不具合修正、タブ、状態復元、その他の順序 |
| 構成の判断 | 現構成を継続するか、ホスト方法などを再検証するか |

基本操作が成立しない場合は、タブ等の追加よりシェルホストの修正・再検証を優先する。

## 参考資料

- [IExplorerBrowser](https://learn.microsoft.com/en-us/windows/win32/api/shobjidl_core/nn-shobjidl_core-iexplorerbrowser)
- [WPFでのWin32コンテンツのホスト](https://learn.microsoft.com/en-us/dotnet/desktop/wpf/advanced/hosting-win32-content-in-wpf)
- [IShellView::TranslateAccelerator](https://learn.microsoft.com/en-us/windows/win32/api/shobjidl_core/nf-shobjidl_core-ishellview-translateaccelerator)
- [IFileOperation](https://learn.microsoft.com/en-us/windows/win32/api/shobjidl_core/nn-shobjidl_core-ifileoperation)

## 更新履歴

| 日付 | 内容 |
|---|---|
| 2026-09-20 | 段階5cのQuickLook連携を追加。選択取得、Spaceを離した時の実行、共通キー設定、起動先検出・手動指定、非同期接続・エラー通知を実装。Core・通信・実画面検証を記録し、次は試用と左サイドビュー（6a） |
| 2026-09-20 | プロジェクト名をexplorer_coverに変更。アプリ表示、C#プロジェクト・名前空間、起動・検証スクリプト、ドキュメントを更新 |
| 2026-09-19 | 初版作成。C#＋WPFを採用し、2ペインの小さな試作と試用を最初の区切りに設定 |
| 2026-09-19 | 段階1〜3を完了。SDK・起動手順・検証記録・試用ランチャーを追加。段階4を試用待ちに更新 |
| 2026-09-19 | 中断後にフォーカス定数を修正。ビルドと起動・終了を再検証。入力送信失敗により段階3の最終キー確認を保留とした |
| 2026-09-19 | 最終キー再検証が成功。初回試用の良好な感想を記録し、タブ→状態復元→再試用の次期計画を提案 |
| 2026-09-20 | 左サイドビュー（ブックマーク・ドライブ容量）、タブ内の移動、QuickLook、キー設定画面を追加。手戻りを抑えるため共通コマンドを先に作り、履歴はタブと同時、サイドビューはレイアウト保存前、設定画面は共通操作・保存の完成後に配置 |
| 2026-09-20 | 段階5aを完了。状態モデル、共通コマンド、JSONキー設定と検証、画面への接続を実装。Coreテスト11件と実画面の初期／変更／不正設定・IME操作を確認 |
| 2026-09-20 | 段階5bを実装・検証。各ペインのタブと履歴移動、キー変更、選択・スクロール保持、ビュー解放を確認。次はユーザー試用と計画見直し |
| 2026-09-20 | タブのマウス操作を段階5b-2として計画追加。ドラッグ並べ替え、対象タブを閉じる操作、空白からの新規タブ作成を、QuickLook・状態保存より前に配置 |
| 2026-09-20 | タブを閉じるマウスボタンを設定可能にする要望を追加。5b-2で設定モデル・JSON読み込み、段階7で変更・保存・初期値復帰の画面を実装する方針 |
| 2026-09-20 | ロック解除後に段階5b-2の検証を完了。全マウスボタン設定・並べ替え・端スクロール・取消・一覧状態保持と既存タブ／IME／ドラッグ移動を確認。次は試用 |
